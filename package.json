{
  "private": true,
  "scripts": {
    "init": "yarn install",
    "all-tsc": "yarn install && tsc -b core data-jdbc test -w -v"
  },
  "workspaces": [
    "types",
    "core",
    "data-jdbc",
    "server",
    "test"
  ]
}